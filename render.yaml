services:
  - type: web
    name: birdride
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: EBIRD_API_KEY
        sync: false  # User must set this manually
    healthCheckPath: /
